[phases.setup]
nixPkgs = ['php84', 'php84Extensions.pdo', 'php84Extensions.pdo_mysql', 'php84Extensions.gd', 'php84Extensions.mbstring', 'php84Extensions.curl', 'php84Extensions.fileinfo', 'php84Extensions.session', 'php84Extensions.tokenizer']

[phases.build]
cmds = ['mkdir -p /app/logs', 'mkdir -p /app/uploads/products']

[start]
cmd = 'php -S 0.0.0.0:${PORT:-8080} -t /app'
